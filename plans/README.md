# Implementation Plans

Generated by the improve skill on 2026-07-03 (audit at commit `221fdf6`, focus: "what can we ship now and how to use it"). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Rename root package to `@oddsquad/tic-tac-token`, make publish-ready, publish | P1 | M | — | DONE (steps 1–7; publish pending operator) |
| 002 | Make `dtcg-tokens-lsp` publish-safe and publish to npm | P1 | S | 001 | TODO |
| 003 | Make the token playground run from a fresh clone | P2 | S | — | TODO |
| 004 | Make the Zed extension work outside this repo's worktree | P3 | M | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

Note: 001 and 002 each end in a **gated publish step** requiring explicit operator confirmation — publishing claims the npm names permanently. Everything before those steps is safe to execute autonomously.

## Dependency notes

- **2026-07-03 rename**: operator chose to publish the root package under the scoped name `@oddsquad/tic-tac-token` (not `dtcg-tokens`). Plan 001 was rewritten accordingly and its scope expanded to the `lsp`/`app` files that import the root package by its bare name. **Plans 002 and 003 still reference the old dependency name `dtcg-tokens` and `pnpm -F dtcg-tokens …`; these must be updated to `@oddsquad/tic-tac-token` before those plans are executed** (partially reconciled during this session — verify before use). Whether the LSP itself moves to the `@oddsquad` scope is an open question deferred to plan 002.
- 002 requires 001: `lsp/package.json` declares the root package as `"workspace:*"`, which pnpm can only rewrite to a real semver range at publish time if `@oddsquad/tic-tac-token` is already on the registry (both names verified 404 on 2026-07-03).
- 004 requires 002: the extension switches from a worktree-relative server path to installing the published `dtcg-tokens-lsp` npm package.
- 003 is independent (workspace-local builds only) but touches root `package.json` scripts — if run concurrently with 001 (also edits root `package.json`, different keys), merge carefully.

## Findings considered and rejected

- **figma-plugin is untouched quickstart boilerplate** (zero references to dtcg anywhere in the directory; "Shape Creator" template): not planned — a real Figma-variables ↔ DTCG bridge is a multi-day design spike, not a fix. Don't present the directory as an integration; if the direction is wanted, spike the read path (Figma variables → DTCG JSON) first.
- **CI workflow (run test + generate-schema + build:dist on push)**: deliberately deferred, not rejected — recommended immediately after first publish (noted in plan 001's maintenance notes). Kept out of 001 to keep its blast radius to package metadata.
- **Resolver unit tests for `aliases.ts`/`clamp.ts`/`extends.ts`/`flatten.ts`/`refs.ts`** (currently covered only via `resolveTokens` integration cases) and **coverage tooling**: real gaps, not ship blockers; revisit when resolver logic next changes.
- **LSP v1 features (go-to-definition, references, document symbols, documentColor)**: accurately documented as unimplemented; the resolver already exposes the reverse-reference graph a references handler would need — good first post-publish feature, but not a readiness issue.
- **`--stdio` handling unverifiable statically** (delegated to `vscode-languageserver` argv defaults): folded into plan 002 Step 4 — the integration tests exercise it end-to-end against the built server.
- **Playground validates against baked-in `schema.json` that could go stale**: mitigation noted in plan 003 maintenance notes; chaining `generate-schema` into every dev run was judged not worth the coupling today.
- **Dual lockfiles (`package-lock.json` + `pnpm-lock.yaml`)**: handled inside plan 001 (Step 5), not a separate plan.
